{
  "name": "openlibrary-playwright-pom",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "npx playwright test",
    "test:headless": "npx playwright test --headed=false",
    "lint": "npx eslint ."
  },
  "devDependencies": {
    "@playwright/test": "^1.44.0",
    "eslint": "^8.54.0",
    "typescript": "^5.6.0"
  }
}
